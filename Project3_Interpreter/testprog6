PROGRAM circle;
	(*Testing If statement condition with equality*)
VAR
	(*Variable declarations*)
	r, p, a : REAL; 
	str : String;
BEGIN
	r := 8;
	p := 'Hello World!' ;
	IF ( p = 5) THEN
	  	a := (3.14) + r * p
	ELSE
		a := p;
		
	(*Display the results*)
	WRITELN  ('The output results are ' , r, ' ', p, ' ', a);
END
